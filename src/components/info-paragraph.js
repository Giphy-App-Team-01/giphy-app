/**
 * Renders an informational paragraph with the provided text.
 *
 * @param {string} text - The text content to be displayed inside the paragraph.
 * @return {string} - A string containing the HTML for the informational paragraph.
 */
export const renderInfoParagraph = (text) => {
  return `<p class="info-paragraph">${text}</p>`;
};
