/**
 * Renders a page title as an HTML heading element.
 *
 * @param {string} text - The text to be displayed as the page title.
 * @return {string} The HTML string for the page title.
 */
export const renderPageTitle = (text) => {
  return `<h2>${text}</h2>`;
};
