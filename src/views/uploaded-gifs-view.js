import { renderPageTitle } from '../components/page-title.js';
import { renderInfoParagraph } from '../components/info-paragraph.js';
export const toUploadedGifsView = (random_id) => {
  return renderPageTitle('Your uploaded GIFs');
};
